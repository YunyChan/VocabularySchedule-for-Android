<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vocabularyschedule"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="14" />

    <application
        android:name="MainApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.vocabularyschedule.ScheduleActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TodayScheduleActivity" />
        <activity android:name=".BookManagerActivity" />
        <activity android:name=".InformationActivity" />
        <activity android:name=".AddBookActivity" />
        <activity android:name=".ReviewModeActivity" />
        <activity
            android:name=".PrefsActivity"
            android:label="系统设置" />
    </application>

</manifest>